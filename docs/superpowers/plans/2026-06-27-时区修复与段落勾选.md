# 时区修复与段落临时勾选 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复统计页时区错位并加近 7 天每日趋势，同时让生成反馈页能临时勾选段落。

**Architecture:** 纯前端改动，无 schema 迁移、无新依赖。三个独立小改动串成 5 个 Task：(1) 抽 `localDay` 时间 helper；(2) `useStats` 用 localDay 并产出 `recent7`；(3) StatsPage 渲染近 7 天 CSS 柱状条；(4) `generatePrompt`/`generateFeedback` 加 `includedSegments` 入参并更新已有测试；(5) GeneratePage 加段落临时勾选 UI + BatchGeneratePage 传全量段落。

**Tech Stack:** React 18 + TypeScript + Vite + Vitest + Tailwind CSS；Intl.DateTimeFormat；Dexie.js。

---

## File Structure

| 文件 | 责任 | 操作 |
|---|---|---|
| `src/lib/time.ts` | `localDay(ts)` 中国时区日期 helper | 新建 |
| `tests/lib/time.test.ts` | localDay 时区边界测试 | 新建 |
| `src/hooks/useStats.ts` | 统计聚合，用 localDay + 产出 recent7 | 修改 |
| `tests/hooks/useStats.test.ts` | recent7/补零/升序/本地日 | 新建 |
| `src/pages/StatsPage.tsx` | 近 7 天 CSS 柱状条 + 历史列表 | 修改 |
| `src/ai/prompts.ts` | generatePrompt 加 includedSegments | 修改 |
| `src/ai/generate.ts` | generateFeedback 透传 includedSegments | 修改 |
| `tests/ai/prompts.test.ts` | generatePrompt 只含勾选段落 | 新建 |
| `tests/ai/generate.test.ts` | 旧测试补 includedSegments 参数 | 修改 |
| `src/pages/GeneratePage.tsx` | segIncluded Set + checkbox + 传参 | 修改 |
| `src/pages/BatchGeneratePage.tsx` | generateFeedback 调用传全量段落 | 修改 |

---

### Task 1: localDay 时间 helper

**Files:**
- Create: `src/lib/time.ts`
- Test: `tests/lib/time.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `tests/lib/time.test.ts`：

```ts
import { describe, it, expect } from "vitest";
import { localDay } from "../../src/lib/time";

describe("localDay", () => {
  // 中国时区 UTC+8，以下用例固定 timestamp，不依赖运行环境
  it("北京时间 2026-06-27 00:00 归为 2026-06-27（非前一天）", () => {
    // UTC 2026-06-26 16:00 = 北京 2026-06-27 00:00
    const ts = Date.UTC(2026, 5, 26, 16, 0, 0);
    expect(localDay(ts)).toBe("2026-06-27");
  });

  it("北京时间 2026-06-26 23:59 归为 2026-06-26", () => {
    const ts = Date.UTC(2026, 5, 26, 15, 59, 0);
    expect(localDay(ts)).toBe("2026-06-26");
  });

  it("北京时间 2026-06-26 08:00 归为 2026-06-26（当天）", () => {
    const ts = Date.UTC(2026, 5, 26, 0, 0, 0);
    expect(localDay(ts)).toBe("2026-06-26");
  });

  it("北京时间 2026-06-26 07:59 归为 2026-06-26（边界，UTC 前一天 23:59）", () => {
    const ts = Date.UTC(2026, 5, 25, 23, 59, 0);
    expect(localDay(ts)).toBe("2026-06-26");
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `cd /workspace && npx vitest run tests/lib/time.test.ts`
Expected: FAIL，`Cannot find module '../../src/lib/time'`

- [ ] **Step 3: 写最小实现**

创建 `src/lib/time.ts`：

```ts
// 显式指定中国时区，不依赖运行环境系统时区
// 教培老师都在中国，固定 Asia/Shanghai 最严谨
const dayFmt = new Intl.DateTimeFormat("zh-CN", {
  timeZone: "Asia/Shanghai",
  year: "numeric", month: "2-digit", day: "2-digit",
});

/** 把毫秒时间戳转成中国时区的 YYYY-MM-DD 日期字符串 */
export function localDay(ts: number): string {
  const parts = dayFmt.formatToParts(new Date(ts));
  const y = parts.find(p => p.type === "year")!.value;
  const m = parts.find(p => p.type === "month")!.value;
  const d = parts.find(p => p.type === "day")!.value;
  return `${y}-${m}-${d}`;
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `cd /workspace && npx vitest run tests/lib/time.test.ts`
Expected: PASS，4 tests passed

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add src/lib/time.ts tests/lib/time.test.ts && GIT_AUTHOR_NAME="trae" GIT_AUTHOR_EMAIL="trae@local" GIT_COMMITTER_NAME="trae" GIT_COMMITTER_EMAIL="trae@local" git commit -m "feat: 加 localDay 中国时区日期 helper"
```

---

### Task 2: useStats 用 localDay + 产出 recent7

**Files:**
- Modify: `src/hooks/useStats.ts`
- Test: `tests/hooks/useStats.test.ts`（新建）

- [ ] **Step 1: 写失败测试**

创建 `tests/hooks/useStats.test.ts`：

```ts
import { describe, it, expect, beforeEach, vi } from "vitest";
import { db } from "../../src/db/schema";
import { getStats } from "../../src/hooks/useStats";

beforeEach(async () => { await db.delete(); await db.open(); });

describe("getStats", () => {
  it("byDay 用中国时区日期（凌晨数据归当天）", async () => {
    // UTC 2026-06-26 16:00 = 北京 2026-06-27 00:00，旧 toISOString 会归 06-26
    await db.tokenUsage.add({ callType: "generate", promptTokens: 10, completionTokens: 5, timestamp: Date.UTC(2026, 5, 26, 16, 0, 0) });
    const s = await getStats();
    expect(s.byDay["2026-06-27"]).toBe(15);
    expect(s.byDay["2026-06-26"]).toBeUndefined();
  });

  it("recent7 长度为 7 且升序", async () => {
    await db.tokenUsage.add({ callType: "generate", promptTokens: 1, completionTokens: 1, timestamp: Date.now() });
    const s = await getStats();
    expect(s.recent7).toHaveLength(7);
    for (let i = 1; i < 7; i++) {
      expect(s.recent7[i].day > s.recent7[i - 1].day).toBe(true);
    }
  });

  it("recent7 自动补零（无数据天 tokens=0）", async () => {
    const s = await getStats();
    expect(s.recent7.every(d => typeof d.tokens === "number")).toBe(true);
    // 全空库，所有天应为 0
    expect(s.recent7.every(d => d.tokens === 0)).toBe(true);
  });

  it("recent7 最后一天是今天", async () => {
    const s = await getStats();
    const { localDay } = await import("../../src/lib/time");
    expect(s.recent7[6].day).toBe(localDay(Date.now()));
  });

  it("total 和 byType 累加正确", async () => {
    await db.tokenUsage.add({ callType: "correct", promptTokens: 10, completionTokens: 5, timestamp: Date.now() });
    await db.tokenUsage.add({ callType: "generate", promptTokens: 20, completionTokens: 10, timestamp: Date.now() });
    const s = await getStats();
    expect(s.total).toBe(45);
    expect(s.byType.correct).toBe(15);
    expect(s.byType.generate).toBe(30);
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `cd /workspace && npx vitest run tests/hooks/useStats.test.ts`
Expected: FAIL，`byDay["2026-06-27"]` undefined（旧 toISOString 归 06-26）；`s.recent7` undefined

- [ ] **Step 3: 修改 useStats.ts**

把 `src/hooks/useStats.ts` 整体替换为：

```ts
import { db } from "../db/schema";
import { CallType } from "../types";
import { localDay } from "../lib/time";

export interface Stats {
  total: number;
  byType: Record<CallType, number>;
  byDay: Record<string, number>;
  recent7: { day: string; tokens: number }[];
}

export async function getStats(): Promise<Stats> {
  const all = await db.tokenUsage.toArray();
  const byType: Record<CallType, number> = {
    correct: 0, generate: 0, learn: 0,
    split: 0, analyzeEdits: 0, extractProfile: 0,
  };
  const byDay: Record<string, number> = {};
  let total = 0;
  for (const u of all) {
    const t = u.promptTokens + u.completionTokens;
    total += t;
    byType[u.callType] += t;
    const day = localDay(u.timestamp);
    byDay[day] = (byDay[day] ?? 0) + t;
  }
  // 近 7 天，升序，无数据天补零
  // 中国无夏令时，now - i*86400000 跨天安全；再用 localDay 归属确保日期正确
  const recent7: { day: string; tokens: number }[] = [];
  const now = Date.now();
  for (let i = 6; i >= 0; i--) {
    const ts = now - i * 24 * 3600 * 1000;
    const day = localDay(ts);
    recent7.push({ day, tokens: byDay[day] ?? 0 });
  }
  return { total, byType, byDay, recent7 };
}
```

- [ ] **Step 4: 跑测试验证通过**

Run: `cd /workspace && npx vitest run tests/hooks/useStats.test.ts`
Expected: PASS，5 tests passed

- [ ] **Step 5: 提交**

```bash
cd /workspace && git add src/hooks/useStats.ts tests/hooks/useStats.test.ts && GIT_AUTHOR_NAME="trae" GIT_AUTHOR_EMAIL="trae@local" GIT_COMMITTER_NAME="trae" GIT_COMMITTER_EMAIL="trae@local" git commit -m "fix: useStats 用 localDay 修时区错位 + 产出 recent7"
```

---

### Task 3: StatsPage 渲染近 7 天 CSS 柱状条

**Files:**
- Modify: `src/pages/StatsPage.tsx`

- [ ] **Step 1: 修改 StatsPage.tsx**

把 `src/pages/StatsPage.tsx` 整体替换为：

```tsx
import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { getStats, Stats } from "../hooks/useStats";
import { Skeleton } from "../components/Skeleton";
import { EmptyState } from "../components/EmptyState";

export default function StatsPage() {
  const [s, setS] = useState<Stats | null>(null);
  useEffect(() => { (async () => setS(await getStats()))(); }, []);
  if (!s) return <Skeleton lines={4} />;
  const max = Math.max(...s.recent7.map(d => d.tokens), 1);
  return (
    <div className="space-y-3">
      <h1 className="text-xl font-bold">Token 消耗统计</h1>
      {s.total === 0 ? (
        <EmptyState title="暂无消耗数据" hint="生成反馈后这里会显示 Token 消耗" action={<Link to="/generate" className="btn-primary">去生成</Link>} />
      ) : (
        <>
          <p className="text-lg">总消耗：<b className="text-blue-700">{s.total}</b> tokens</p>
          <div className="card">
            <h2 className="font-semibold">近 7 天</h2>
            <div className="flex items-end gap-2 h-32 mt-2">
              {s.recent7.map(d => (
                <div key={d.day} className="flex-1 flex flex-col items-center justify-end h-full">
                  <span className="text-xs text-gray-600 mb-0.5">{d.tokens}</span>
                  <div
                    className="w-full bg-blue-400 rounded-t"
                    style={{ height: `${(d.tokens / max) * 100}%`, minHeight: d.tokens > 0 ? "2px" : "0" }}
                    title={`${d.day}: ${d.tokens}`}
                  />
                  <span className="text-xs text-gray-500 mt-1">{d.day.slice(5)}</span>
                </div>
              ))}
            </div>
          </div>
          <div className="card">
            <h2 className="font-semibold">按类型</h2>
            <ul className="text-sm">
              {Object.entries(s.byType).map(([k, v]) => <li key={k} className="py-0.5">{k}：{v}</li>)}
            </ul>
          </div>
          <div className="card">
            <h2 className="font-semibold">全部历史（按日期）</h2>
            {Object.keys(s.byDay).length === 0 ? (
              <p className="text-sm text-gray-500">暂无数据</p>
            ) : (
              <ul className="text-sm">{Object.entries(s.byDay).map(([d, t]) => <li key={d} className="py-0.5">{d}：{t}</li>)}</ul>
            )}
          </div>
        </>
      )}
    </div>
  );
}
```

- [ ] **Step 2: tsc + build 验证**

Run: `cd /workspace && npm run build 2>&1 | tail -8`
Expected: build 成功，无 TS 错误

- [ ] **Step 3: 跑全量测试确认无回归**

Run: `cd /workspace && npx vitest run 2>&1 | tail -8`
Expected: 全部通过

- [ ] **Step 4: 提交**

```bash
cd /workspace && git add src/pages/StatsPage.tsx && GIT_AUTHOR_NAME="trae" GIT_AUTHOR_EMAIL="trae@local" GIT_COMMITTER_NAME="trae" GIT_COMMITTER_EMAIL="trae@local" git commit -m "feat: StatsPage 近 7 天 CSS 柱状趋势"
```

---

### Task 4: generatePrompt / generateFeedback 加 includedSegments

**Files:**
- Modify: `src/ai/prompts.ts`
- Modify: `src/ai/generate.ts`
- Test: `tests/ai/prompts.test.ts`（新建）
- Modify: `tests/ai/generate.test.ts`（补参数）

- [ ] **Step 1: 写失败测试 prompts.test.ts**

创建 `tests/ai/prompts.test.ts`：

```ts
import { describe, it, expect } from "vitest";
import { generatePrompt } from "../../src/ai/prompts";
import { SpecProfile, Student } from "../../src/types";

const profile: SpecProfile = {
  subject: "数学", name: "p", tone: "正式书面", styleNote: "s",
  segments: [
    { title: "课堂内容", targetWords: 80, contentPoints: "知识点", freeNote: "" },
    { title: "学生表现", targetWords: 100, contentPoints: "专注度", freeNote: "" },
    { title: "家庭建议", targetWords: 60, contentPoints: "练习", freeNote: "" },
  ],
  opening: "该生今日", ending: "望配合", lockedFields: [], isBuiltin: false, createdAt: 0,
};
const student: Student = { name: "张三", grade: "初三", personality: "", weaknesses: "", parentFocus: "", defaultSubject: "", createdAt: 0 };

describe("generatePrompt", () => {
  it("includedSegments 只含部分段落时，system 只含这些段落标题", () => {
    const included = [profile.segments[0], profile.segments[2]];
    const msgs = generatePrompt(profile, student, "课程", [], included);
    const sys = msgs[0].content;
    expect(sys).toContain("课堂内容");
    expect(sys).not.toContain("学生表现");
    expect(sys).toContain("家庭建议");
  });

  it("includedSegments 为全部段落时，system 含全部标题", () => {
    const included = profile.segments;
    const msgs = generatePrompt(profile, student, "课程", [], included);
    const sys = msgs[0].content;
    expect(sys).toContain("课堂内容");
    expect(sys).toContain("学生表现");
    expect(sys).toContain("家庭建议");
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `cd /workspace && npx vitest run tests/ai/prompts.test.ts`
Expected: FAIL，`generatePrompt` 签名不匹配（缺第 5 个参数会报 TS 错或行为不符）

- [ ] **Step 3: 修改 prompts.ts**

把 `src/ai/prompts.ts` 的 `generatePrompt` 函数替换为（其余函数不动）：

```ts
export function generatePrompt(
  profile: SpecProfile,
  student: Student,
  courseContent: string,
  history: Feedback[],
  includedSegments: SpecSegment[],
) {
  const segDesc = includedSegments.map((s, i) =>
    `第${i + 1}段「${s.title}」约${s.targetWords}字，要点：${s.contentPoints}${s.freeNote ? "；补充：" + s.freeNote : ""}`
  ).join("\n");
  const histTxt = history.slice(-2).map((h, i) => `历史反馈${i + 1}：${h.finalText}`).join("\n\n") || "（无历史反馈）";
  const system = `你是教培课后反馈撰写助手。严格按以下规范档撰写反馈。
语气：${profile.tone}；风格说明：${profile.styleNote}
段落结构：
${segDesc}
开头：${profile.opening}
结尾：${profile.ending}
${JSON_INSTRUCTION}
输出 JSON 格式：{"feedback":"整篇反馈正文"}`;
  const user = `学生姓名：${student.name}；年级：${student.grade}；性格：${student.personality}；薄弱点：${student.weaknesses}；家长关注：${student.parentFocus}
科目：${profile.subject}
本节课内容（老师口述/输入）：${courseContent}
${histTxt}`;
  return [{ role: "system", content: system }, { role: "user", content: user }];
}
```

注意：`SpecSegment` 已在文件顶部 import（`import { SpecProfile, Student, Feedback } from "../types";`）。需确认 import 含 `SpecSegment`，若无需补。实际现有 import 行是 `import { SpecProfile, Student, Feedback } from "../types";`，要改成 `import { SpecProfile, SpecSegment, Student, Feedback } from "../types";`。

- [ ] **Step 4: 修改 generate.ts 透传**

把 `src/ai/generate.ts` 整体替换为：

```ts
// src/ai/generate.ts
import { callDeepSeek } from "./client";
import { generatePrompt } from "./prompts";
import { parseJsonLoose } from "./parse";
import { SpecProfile, SpecSegment, Student, Feedback } from "../types";

export async function generateFeedback(args: {
  apiKey: string; profile: SpecProfile; student: Student; courseContent: string; history: Feedback[];
  includedSegments: SpecSegment[];
}): Promise<{ feedback: string }> {
  const messages = generatePrompt(args.profile, args.student, args.courseContent, args.history, args.includedSegments);
  const res = await callDeepSeek({ apiKey: args.apiKey, model: "deepseek-chat", messages, responseFormatJson: true }, "generate");
  const parsed = parseJsonLoose(res.content);
  return { feedback: String(parsed.feedback ?? "") };
}
```

- [ ] **Step 5: 修改 tests/ai/generate.test.ts 补参数**

把 `tests/ai/generate.test.ts` 两处 `generateFeedback({ ... })` 调用补 `includedSegments`。

第 14 行（第一个测试）：

```ts
    const out = await generateFeedback({ apiKey: "k", profile, student, courseContent: "讲了幂函数", history: [] as Feedback[], includedSegments: profile.segments });
```

第 23 行（第二个测试）：

```ts
    const out = await generateFeedback({ apiKey: "k", profile, student, courseContent: "x", history: [], includedSegments: profile.segments });
```

- [ ] **Step 6: 跑测试验证通过**

Run: `cd /workspace && npx vitest run tests/ai/prompts.test.ts tests/ai/generate.test.ts 2>&1 | tail -10`
Expected: PASS，4 tests passed（prompts 2 + generate 2）

- [ ] **Step 7: 跑全量测试 + tsc build 确认无回归**

Run: `cd /workspace && npx vitest run 2>&1 | tail -8 && npm run build 2>&1 | tail -8`
Expected: 全部测试通过，build 成功

注意：此时 GeneratePage/BatchGeneratePage 还没改，会 TS 编译报错（调用 generateFeedback 缺 includedSegments）。这是预期的中间态。为让 build 通过，本 Task 暂时在两个 page 的调用处传 `includedSegments: profile.segments`（全量，保持原行为），下个 Task 再改 GeneratePage 的勾选逻辑。

具体：先在本 Step 临时修改 `src/pages/GeneratePage.tsx` 第 75 行和 `src/pages/BatchGeneratePage.tsx` 第 94 行，调用 `generateFeedback` 时加 `includedSegments: profile.segments`。下个 Task 6 会把 GeneratePage 的改成交互式勾选。

- [ ] **Step 8: 提交**

```bash
cd /workspace && git add src/ai/prompts.ts src/ai/generate.ts tests/ai/prompts.test.ts tests/ai/generate.test.ts src/pages/GeneratePage.tsx src/pages/BatchGeneratePage.tsx && GIT_AUTHOR_NAME="trae" GIT_AUTHOR_EMAIL="trae@local" GIT_COMMITTER_NAME="trae" GIT_COMMITTER_EMAIL="trae@local" git commit -m "feat: generatePrompt/generateFeedback 加 includedSegments 入参"
```

---

### Task 5: GeneratePage 段落临时勾选 UI

**Files:**
- Modify: `src/pages/GeneratePage.tsx`

- [ ] **Step 1: 修改 GeneratePage.tsx**

在 `src/pages/GeneratePage.tsx` 做以下改动：

**1a. 加 segIncluded state（在 generating state 之后）：**

在 `const [generating, setGenerating] = useState(false);` 之后加：

```ts
  const [segIncluded, setSegIncluded] = useState<Set<number>>(new Set());
```

**1b. 加 useEffect 在 profileId 变化时重置全选：**

在现有的 `useEffect(() => { setAvailableProfiles(profiles); }, [profiles]);` 之后（约第 37 行后）加：

```ts
  useEffect(() => {
    const p = profiles.find(p => p.id === profileId);
    if (p) setSegIncluded(new Set(p.segments.map((_, i) => i)));
    else setSegIncluded(new Set());
  }, [profileId, profiles]);
```

**1c. doGenerate 用 includedSegments：**

把 `doGenerate` 函数中调用 `generateFeedback` 那段（约第 73-76 行）替换为：

```ts
    const includedSegments = profile.segments.filter((_, i) => segIncluded.has(i));
    if (includedSegments.length === 0) { notify.error("请至少选择一个段落"); return; }
    try {
      const history = student.id ? await listFeedbacksByStudent(student.id) : [];
      const out = await generateFeedback({ apiKey, profile, student, courseContent: text, history, includedSegments });
```

注意：`setGenerating(true); setPreview("");` 那行保留在前面。完整 doGenerate 应为：

```ts
  const doGenerate = async () => {
    const apiKey = await getApiKey();
    if (!apiKey) { notify.error("请先设置 API Key"); return; }
    const student = students.find(s => s.id === studentId);
    const profile = profiles.find(p => p.id === profileId);
    if (!student || !profile) { notify.error("请选择学生和规范档"); return; }
    const includedSegments = profile.segments.filter((_, i) => segIncluded.has(i));
    if (includedSegments.length === 0) { notify.error("请至少选择一个段落"); return; }
    setGenerating(true); setPreview("");
    const tid = notify.info("生成中…", { duration: 0 });
    try {
      const history = student.id ? await listFeedbacksByStudent(student.id) : [];
      const out = await generateFeedback({ apiKey, profile, student, courseContent: text, history, includedSegments });
      setPreview(out.feedback); setEditing(out.feedback);
      notify.dismiss(tid);
      notify.success("生成完成");
    } catch (e: any) { notify.dismiss(tid); notify.error("生成失败：" + e.message + "（可重试或手动编写）"); }
    finally { setGenerating(false); }
  };
```

"重新生成"按钮（preview 区，约第 134 行）调用的也是 doGenerate，保留当前 segIncluded（不重置），用户调整勾选后重新生成应生效——符合需求。

**1d. 加段落勾选 UI：**

在规范档下拉的 `</select>` 之后（约第 106 行后，`</div>` 闭合 grid 之前或之后），插入：

```tsx
        {(() => {
          const p = profiles.find(p => p.id === profileId);
          if (!p || p.segments.length === 0) return null;
          return (
            <div className="card p-2 space-y-1">
              <p className="text-sm font-semibold">本次段落（默认全选，可临时取消）</p>
              {p.segments.map((s, i) => (
                <label key={i} className="flex items-center gap-2 text-sm">
                  <input
                    type="checkbox"
                    checked={segIncluded.has(i)}
                    onChange={() => setSegIncluded(prev => {
                      const next = new Set(prev);
                      if (next.has(i)) next.delete(i); else next.add(i);
                      return next;
                    })}
                  />
                  <span>{s.title || "（无标题）"}（约{s.targetWords}字）</span>
                </label>
              ))}
            </div>
          );
        })()}
```

插入位置：在 `<div className="grid grid-cols-2 gap-2">...</div>` 这个 grid 闭合 `</div>` 之后、`<div className="space-y-2">` 之前。

- [ ] **Step 2: tsc + build 验证**

Run: `cd /workspace && npm run build 2>&1 | tail -8`
Expected: build 成功，无 TS 错误

- [ ] **Step 3: 跑全量测试确认无回归**

Run: `cd /workspace && npx vitest run 2>&1 | tail -8`
Expected: 全部通过

- [ ] **Step 4: 提交**

```bash
cd /workspace && git add src/pages/GeneratePage.tsx && GIT_AUTHOR_NAME="trae" GIT_AUTHOR_EMAIL="trae@local" GIT_COMMITTER_NAME="trae" GIT_COMMITTER_EMAIL="trae@local" git commit -m "feat: GeneratePage 段落临时勾选 UI（不持久化，切档重置）"
```

---

## 完成后验证（非 Task，最后一步）

- [ ] **全量测试 + tsc + build**

```bash
cd /workspace && npx vitest run 2>&1 | tail -8 && npm run build 2>&1 | tail -8
```

- [ ] **推送部署 + 线上验证**

```bash
cd /workspace && git push origin master
```

轮询 `https://api.github.com/repos/qbjsdsb/classroomfeedback/actions/runs?per_page=1` 等 conclusion=success，然后用 Playwright 验证：
1. 统计页显示近 7 天柱状条
2. 生成页选规范档后显示段落勾选区，默认全选
3. 取消某段落 → 生成 → 无报错
4. 切规范档 → 勾选重置

---

## Self-Review 自检

**1. Spec coverage：**
- 时区修复（spec §4）→ Task 1 + Task 2 ✓
- 近 7 天趋势（spec §5）→ Task 2 + Task 3 ✓
- 段落勾选数据契约（spec §6.1-6.3）→ Task 4 ✓
- GeneratePage UI（spec §6.4）→ Task 5 ✓
- BatchGeneratePage 传全量（spec §6.5）→ Task 4 Step 7 临时传全量 ✓（Task 5 不动 BatchGeneratePage，保持全量）
- 测试策略（spec §9）→ 各 Task 内 TDD ✓
- 边界：空集拦截 → Task 5 Step 1c ✓；切档重置 → Task 5 Step 1b ✓；重新生成保留 → Task 5 doGenerate 不重置 segIncluded ✓；批量全量 → Task 4 Step 7 ✓

**2. Placeholder scan：** 无 TBD/TODO，所有步骤含完整代码 ✓

**3. Type consistency：**
- `localDay(ts: number): string` — Task 1 定义，Task 2 使用 ✓
- `recent7: { day: string; tokens: number }[]` — Task 2 定义，Task 3 使用 ✓
- `includedSegments: SpecSegment[]` — Task 4 定义，Task 5 使用 ✓
- `segIncluded: Set<number>` — Task 5 内一致 ✓
- `generateFeedback` 入参 `includedSegments` — Task 4 定义，Task 5 doGenerate 传 ✓
- `generatePrompt` 第 5 参数 `includedSegments: SpecSegment[]` — Task 4 定义，Task 4 测试使用 ✓

**自检发现并修正：**
- Task 4 Step 7 注意点已补：GeneratePage/BatchGeneratePage 此时未改会 TS 报错，临时传全量 `profile.segments` 让 build 通过，Task 5 再改 GeneratePage 交互。避免中间态 build 失败。
- Task 4 Step 3 注意点已补：prompts.ts import 需加 `SpecSegment`。
